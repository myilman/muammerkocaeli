#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

//This program creates 15 random integer

int main()
{
    int i=0;
    srand(time(NULL));
    while(i<15){
    printf("%d \n", rand());
    i++;
    }
    return 0;
}
